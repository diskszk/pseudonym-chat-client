import type { NextPage } from "next";
import { Layout } from "../shared";

const Home: NextPage = () => {
  return (
    <div>
      <Layout>
        <p>hello world</p>
      </Layout>
    </div>
  );
};

export default Home;
